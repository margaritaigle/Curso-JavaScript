const producto = 'Monitor 20 Pulgadas';

console.log(producto);

// Conocer la cantidad de letras de la cadena de texto (cuenta los espacios)
console.log(producto.length);

// Conocer si la palabra existe (nos indica la posición de la letra el console.log)
// -1 es que no lo encontró
console.log(producto.indexOf('20'));

// Conocer si la palabra existe, nos da un valor booleano
// DISTINGUE ENTRE MAYÚSCULAS  MINÚSCULAS
console.log(producto.includes('Monitor'));

