const numero1 = 20;
const numero2 = '20';

console.log( parseInt(numero2)); // Es una funcion, se manda a llamar 
// a la funcion y se le pasa un valor dentro (numero2) es un argumento


console.log( numero1.toString() ) // Es un metodo

// Son basicamente lo mismo



function sumar() {
    console.log( 2 + 2);
}

sumar()