const numeros = [10, 20, 30];

const meses = new Array('Enero', 'Febrero', 'Marzo'); // con paréntesis

console.log(numeros);
console.log(meses);

// Arreglo que contiene datos de todo tipo

const deTodo = ['Hola', 10, true, 'si', null, { nombre: 'Juan', trabajo: 'Programador'}, [1, 2, 3]];
console.log(deTodo);

// es una variable o forma que va a contener múltiples elementos

